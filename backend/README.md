# Backend Skeleton

This directory is reserved for the Java Spring Boot API.

Planned modules:

- Profile API
- Project API
- Skill API
- Message API
- Admin login
- MySQL persistence

## Local setup later

Install JDK 17+ and Maven, then configure MySQL:

```powershell
cd backend
mvn spring-boot:run
```

